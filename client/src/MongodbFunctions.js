export const postNewItem = (itemName, itemId, weight, outByDate) => {
    console.log("Need to implement post to MongoDB", itemName, itemId, weight, outByDate);
    // fetch('url', {
    //     method: 'POST',
    //     body: JSON.stringify({
    //         itemName: itemName,
    //         itemId: itemId,
    //         weight: weight,
    //         outByDate: outByDate
    //     })
    // }) 
}